# Test Strategy — ProofDesk

Unit tests:
- release-state computation
- status transitions

Integration tests:
- API execution runner
- localStorage persistence

Acceptance tests:
- hierarchy rendering
- function workflow
- approval trigger workflow

Release logic tests:
commit blocked when approval flag missing
commit blocked when blocking functions exist
